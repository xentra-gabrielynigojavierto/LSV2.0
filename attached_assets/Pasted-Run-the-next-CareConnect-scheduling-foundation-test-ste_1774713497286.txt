Run the next CareConnect scheduling foundation test step: create and verify the first Service Offering.

Goal:
Verify that the CareConnect Service Offerings endpoint can successfully create tenant-scoped service offering data through Gateway.

Context:
- Gateway, Identity, and CareConnect are already running
- Scheduling foundation migration is already applied
- Facility creation has already been tested successfully
- This step is only for Service Offering creation and basic authorization verification
- Do not add new features or change schema in this step

Tasks

1. Authenticate through Gateway using the existing admin account:
- POST /identity/api/auth/login

Use:
- tenantCode = LEGALSYNQ
- email = admin@legalsynq.com
- password = ChangeMe123!

2. Capture the accessToken from the login response.

3. Use the token to call:
- POST /careconnect/api/service-offerings

Create one Service Offering with this sample payload:
{
  "name": "Physical Therapy Evaluation",
  "code": "PT_EVAL",
  "description": "Initial physical therapy evaluation",
  "durationMinutes": 30,
  "isActive": true
}

4. Verify the response:
- returns 200 or 201
- contains a ServiceOfferingResponse
- includes TenantId
- includes the values submitted

5. Also verify:
- the request succeeds only with a valid admin token
- a request without token returns 401
- a request with a StandardUser token returns 403 if current authorization rules require PlatformOrTenantAdmin for create

6. Verify duplicate code behavior
Attempt to create the same Service Offering again with the same code:
- PT_EVAL

Expected:
- clean failure
- ideally 400 or a handled conflict-style response depending on the current exception handling pattern
- no raw database exception leaked

7. Output:
- login request used
- service offering create request used
- whether creation succeeded
- the returned Service Offering Id
- whether duplicate code was handled correctly
- whether 401/403 behavior was correct

Important:
Do not create availability templates yet.
Do not add new code unless something is broken.
This step is only to validate auth and Service Offering creation.