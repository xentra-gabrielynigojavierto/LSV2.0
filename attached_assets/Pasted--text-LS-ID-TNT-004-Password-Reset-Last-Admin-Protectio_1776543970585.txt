```text
# LS-ID-TNT-004 — Password Reset + Last Admin Protection

## BUILD INSTRUCTIONS (STRICTLY FOLLOW)

You are implementing the next incremental tenant user management hardening feature on top of:
- LS-ID-TNT-001 — Tenant Users List Stabilization
- LS-ID-TNT-002 — Add User Flow
- LS-ID-TNT-003 — User Actions (Edit + Status Control)

This feature MUST:
- Be incremental (NO rewrites)
- Reuse existing components/services
- Preserve all existing list/search/filter/add/edit/status behavior
- Respect tenant scoping (JWT/session-based)
- Include explicit fallback behavior
- NOT introduce placeholder logic
- Validate the actual current codebase before implementing
- Prioritize safe enforcement over optimistic assumptions

---

## FEATURE OBJECTIVE

Close the two highest-risk security and operability gaps in tenant user management by adding:
- Reset Password
- Last Admin Protection

This feature should make tenant user administration safer and more supportable without expanding into invite onboarding, bulk import, or broader access model changes.

---

## HARD SCOPE BOUNDARY (DO NOT VIOLATE)

DO NOT IMPLEMENT:
- Invite-based onboarding
- First-login password reset flow unless it already exists and is trivially exposable
- Bulk import
- Product assignment
- Group assignment
- Tenant-scoped role redesign
- Audit dashboard/reporting UI
- Any redesign of page layout
- Any changes to Groups / Access / Simulator tabs

ONLY IMPLEMENT:
- Reset Password row action
- Reset Password confirmation and result handling
- Reset Password API integration using the real backend contract
- Last-admin protection analysis and implementation
- Safe prevention of deactivating the last active tenant admin
- Safe prevention of removing the last active tenant admin’s admin access if current edit flow makes that possible
- Clear blocked-action messaging
- Regression-safe integration with the existing Users page

---

## STEP 0 — CREATE REPORT (MANDATORY FIRST STEP)

Create file:
/analysis/LS-ID-TNT-004-report.md

Initialize with:

# LS-ID-TNT-004 — Password Reset + Last Admin Protection

## 1. Executive Summary
## 2. Codebase Analysis
## 3. Existing Reset Password Contract
## 4. Existing Last Admin Protection Analysis
## 5. Admin Role Determination Analysis
## 6. Files Changed
## 7. UI Implementation
## 8. Reset Password UX / Result Handling
## 9. Last Admin Protection Implementation
## 10. API Integration
## 11. Tenant Scope Handling
## 12. Testing Results
## 13. Known Issues / Gaps
## 14. Final Status

IMPORTANT:
- Create this report FIRST
- Update it incrementally after each major step
- Do NOT wait until the end

---

## STEP 1 — CODEBASE ANALYSIS

Inspect the current codebase and document:

- Current Users page entry point
- Current row actions menu implementation
- Existing Edit User modal behavior
- Existing activate/deactivate integration
- Whether an admin-triggered reset-password endpoint already exists
- Whether reset-password returns:
  - a temporary password
  - a success message only
  - an email dispatch result
  - no useful payload
- Whether password reset is admin-only, self-service only, or both
- Whether last-admin protection exists anywhere in backend
- How admin-capable roles are determined in the current role model
- Whether LS-ID-TNT-003 role-editing can remove the last active admin path
- Whether self-deactivation/self-role-downgrade are currently possible
- Whether there is any backend helper or policy for “assignable roles” or “admin roles”

DO NOT assume any contract or behavior until validated from actual code.

Update report:
## 2. Codebase Analysis
## 3. Existing Reset Password Contract
## 4. Existing Last Admin Protection Analysis
## 5. Admin Role Determination Analysis

---

## STEP 2 — ADD RESET PASSWORD ROW ACTION

Extend the existing per-row actions menu.

Requirements:
- Add “Reset Password”
- Keep existing actions intact:
  - View
  - Edit
  - Activate / Deactivate
- The menu must remain compact and consistent with current table UX
- Do not break existing list layout or row interaction

Update report:
## 7. UI Implementation

---

## STEP 3 — IMPLEMENT RESET PASSWORD CONFIRMATION FLOW

Add a confirmation step before reset.

Requirements:
- Use existing modal/confirm dialog patterns where possible
- Message must align to the actual backend behavior discovered in Step 1

Examples:
- “Reset this user’s password? A temporary password will be generated.”
- “Reset this user’s password? Reset instructions will be sent to the user.”
- “Reset this user’s password? Their existing password will no longer work.”

DO NOT hardcode messaging that contradicts the actual backend flow.

Update report:
## 7. UI Implementation
## 8. Reset Password UX / Result Handling

---

## STEP 4 — IMPLEMENT RESET PASSWORD API INTEGRATION

Integrate Reset Password using the actual existing backend contract discovered in Step 1.

Rules:
- Do NOT invent a new endpoint unless absolutely necessary and justified
- Reuse existing API client/BFF patterns
- Preserve tenant-safe behavior
- Keep payload aligned to the real backend contract

Handle all realistic backend outcomes:
- temporary password returned
- success message returned
- no payload returned
- email-dispatch result returned
- unauthorized / forbidden
- validation failure
- network/server failure

Success behavior:
- show clear success feedback
- if a temporary password is returned, display it clearly and safely
- do not silently discard sensitive result data if the admin needs it
- preserve page usability after completion

Failure behavior:
- preserve page context
- show user-friendly error feedback
- do not silently fail

Update report:
## 8. Reset Password UX / Result Handling
## 10. API Integration

---

## STEP 5 — ANALYZE LAST ADMIN RISK PATHS

Validate and document all realistic lockout paths:

- Deactivating the last active tenant admin
- Self-deactivating as the last active tenant admin
- Removing the last active tenant admin’s admin-capable role
- Self-role-downgrade from the last active admin path
- Any status or role combination that leaves the tenant with zero active admins

You must identify:
- what data exists to determine admin status reliably
- whether current role names/IDs allow reliable identification
- whether backend already blocks any of these cases
- whether frontend-only protection can be made trustworthy

Update report:
## 4. Existing Last Admin Protection Analysis
## 5. Admin Role Determination Analysis

---

## STEP 6 — IMPLEMENT LAST ADMIN PROTECTION

Implement the safest protection possible based on validated codebase findings.

Priority order:
1. Reuse backend protection if it already exists
2. If backend does not exist but reliable frontend determination is possible, implement frontend prevention
3. If neither is fully reliable, fail safely and clearly document the remaining backend gap

Minimum requirements:
- Prevent deactivation of the last active tenant admin
- If current edit flow can remove the last admin-capable role, prevent that too
- Surface blocked-action messaging clearly

Examples of blocked messaging:
- “This action is not allowed because this is the last active tenant admin.”
- “This role change is not allowed because it would remove the final active tenant admin.”

DO NOT implement a fake or weak heuristic that could silently allow lockout while pretending to be safe.

Update report:
## 9. Last Admin Protection Implementation
## 10. API Integration
## 13. Known Issues / Gaps

---

## STEP 7 — HANDLE SELF-PROTECTION SCENARIOS

Ensure the system safely handles:
- self-deactivation
- self-role-downgrade if supported in current UI
- resetting one’s own password if the backend allows it

Minimum required:
- last-admin self-deactivation must not create lockout
- last-admin self-role-downgrade must not create lockout if current role-edit flow allows it

If the backend contract or current UI does not support one of these scenarios, document it explicitly.

Update report:
## 9. Last Admin Protection Implementation
## 12. Testing Results

---

## STEP 8 — TENANT SAFETY

Ensure:
- no client-driven tenant switching
- no cross-tenant reset password action
- no cross-tenant last-admin evaluation
- any tenant identifiers used are sourced from authenticated session and/or validated by backend
- existing tenant safety guarantees from LS-ID-TNT-001 / 002 / 003 remain intact

Update report:
## 11. Tenant Scope Handling

---

## STEP 9 — REGRESSION PROTECTION

Ensure this feature does NOT break:
- LS-ID-TNT-001 list/search/filter/loading/error behavior
- LS-ID-TNT-002 Add User flow
- LS-ID-TNT-003 Edit / Activate / Deactivate flows
- null-safe rendering
- pagination, if present

Do not reintroduce unsafe assumptions or UI instability.

Update report:
## 12. Testing Results

---

## STEP 10 — TESTING

Test and document each of the following:

### Reset Password
- Reset password for a normal active user
- Reset password for an inactive user, if backend allows
- Successful reset with returned credential/message
- Reset password unauthorized / forbidden
- Reset password validation failure
- Reset password network/server failure

### Last Admin Protection
- Attempt to deactivate the last active tenant admin
- Attempt to deactivate one admin when another active admin still exists
- Attempt self-deactivation as last active admin
- Attempt to remove admin role from the last active admin if current edit flow allows it
- Attempt self-role-downgrade as last active admin if current edit flow allows it

### Regression
- Users list still loads
- Search still works
- Filters still work
- Add User still works
- Edit User still works
- Activate/Deactivate still works for allowed cases
- Null-safe rendering remains intact

Update report:
## 12. Testing Results

---

## STEP 11 — FINALIZE REPORT

Complete:
## 1. Executive Summary
## 13. Known Issues / Gaps
## 14. Final Status = Complete

Your final report must clearly state:
- What reset-password contract was found
- What the reset flow returns on success
- Whether temporary credentials are exposed or not
- Whether last-admin protection is backend-enforced, frontend-enforced, both, or still a gap
- How admin-capable roles are determined
- Whether self-deactivation/self-role-downgrade are safely handled
- Whether any remaining lockout risk still exists

---

## SUCCESS CRITERIA

✔ Row actions include Reset Password
✔ Reset Password uses confirmation
✔ Reset Password integrates with the real backend contract
✔ Successful reset shows correct feedback based on actual backend behavior
✔ Failed reset shows useful error feedback without breaking context
✔ Deactivation of the last active tenant admin is prevented safely
✔ Removal of the last active tenant admin’s admin access is prevented safely if current UI/backend makes that possible
✔ Self-deactivation / self-admin-removal scenarios are handled safely
✔ No regression to LS-ID-TNT-001, LS-ID-TNT-002, or LS-ID-TNT-003

---

## FINAL DELIVERABLES

1. Working Reset Password flow
2. Working Last Admin Protection
3. Completed report:
/analysis/LS-ID-TNT-004-report.md

---

END OF IMPLEMENTATION
```
