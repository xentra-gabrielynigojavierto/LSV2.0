apiVersion: v1
kind: Service

metadata:
 name: gateway-yarp
 namespace: dev
 annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing

 #labels:
 #app: dotnet-core-service

spec:
  type: LoadBalancer
  selector:
   app: gateway-yarp

  ports:
  - port: 80
    targetPort: 5010   #change per app