/**
 * Blank placeholder body for routes that are not yet implemented.
 * Renders an empty white area — no error, no content.
 */
export function BlankPage() {
  return <div className="w-full h-full bg-white" />;
}
