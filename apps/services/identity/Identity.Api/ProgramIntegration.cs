// This partial declaration makes the top-level Program class visible to
// the integration test project (WebApplicationFactory<Program>).
// It is intentionally empty — all app setup lives in Program.cs.
public partial class Program { }
